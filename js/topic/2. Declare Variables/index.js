const declare = () => {
  /** To do
   * declare a constant variable named 'mySchool' with a value of 'Holy Trinity College of General Santos City Inc.'
   * declare a variable named 'myCourse'
   * assign value to 'myCourse' variable with either of the three: "BSIT", "BSCS", "BSCpE"
   *
   * Please do use double quote
   */

  const mySchool = "Holy Trinity College of General Santos City Inc.";
  let myCourse;
  myCourse = "BSCpE";

  //   do not change the return statement
  return {
    mySchool,
    myCourse,
  };
};

module.exports = declare;
